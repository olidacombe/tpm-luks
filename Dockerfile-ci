FROM ubuntu:22.04

RUN apt-get update && \
    apt-get install -y \
	build-essential \
	cryptsetup-bin \
	curl \
	libcryptsetup-dev \
	libtss2-dev \
	pkg-config \
	swtpm \
	swtpm-tools \
	tss2

RUN mkdir /workdir
WORKDIR /workdir

COPY make-luks-image.sh .
